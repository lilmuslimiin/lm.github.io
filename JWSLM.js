/* Copyright [lilmuslimin.com] [2024] */
document.addEventListener('DOMContentLoaded', function () { const daysOfWeek = ['Minggu', 'Senin', 'Selasa', 'Rabu', 'Kamis', 'Jumat', 'Sabtu']; const rows = document.querySelectorAll('tbody tr'); rows.forEach(row => { const rowTanggal = parseInt(row.getAttribute('data-tanggal'), 10); const rowBulan = parseInt(row.getAttribute('data-bulan'), 10) - 1; const rowTahun = parseInt(row.getAttribute('data-tahun'), 10); const date = new Date(rowTahun, rowBulan, rowTanggal); const dayIndex = date.getDay(); row.cells[0].textContent = daysOfWeek[dayIndex]; if (rowTanggal === new Date().getDate() && rowBulan === new Date().getMonth() && rowTahun === new Date().getFullYear()) { row.classList.add('bg-primary', 'text-white'); } }); });